const CounterDisplay = ({ count }) => {
    // Il valore del contatore viene passato come una prop e viene mostrato in un elemento h2.
    return <h2>Contatore: {count}</h2>;
  };

export default CounterDisplay;
