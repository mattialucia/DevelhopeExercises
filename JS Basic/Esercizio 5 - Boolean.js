let itIsRaining = true;
let itIsSnowing = false;
let itIsRunning = false;
let itIsSleeping = true;
let itIsEating = false;

console.log(`\nSta piovendo? ${itIsRaining}.\nSta nevicando? ${itIsSnowing}.\nSta correndo? ${itIsRunning}.\nSta dormendo? ${itIsSleeping}.\nSta mangiando? ${itIsEating}.`);